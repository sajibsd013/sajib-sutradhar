import NEUBData from '../Data/NEUBData';
import AboutData from '../Data/AboutData';

const initialState = {
    NEUBData: NEUBData,
    AboutData: AboutData
}

export const reducer = (state = initialState, action) => {
    return state;
}